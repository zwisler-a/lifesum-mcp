# Optional one-time bootstrap token; prefer the register_lifesum_token tool instead.
LIFESUM_TOKEN=eyJhbGciOi...

# Optional shared secret required as a Bearer token on /mcp requests.
MCP_AUTH_TOKEN=

PORT=3000
HOST=127.0.0.1
